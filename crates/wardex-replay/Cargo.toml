[package]
name                   = "wardex-replay"
version                = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
authors.workspace      = true
license.workspace      = true
description            = "Replay canonicalization + hashing for Wardex SDK"

[dependencies]
wardex-codec = { path = "../wardex-codec" }
