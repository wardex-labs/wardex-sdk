//! Wardex replay — canonicalization and hashing for deterministic replay.
//!
//! Empty: the canonicalization rule lives in `sdks/python/src/wardex_sdk/_hash.py`
//! today, and this crate reserves the place a byte-exact port of it would go.

#[cfg(test)]
mod tests {
    #[test]
    fn it_compiles() {}
}
